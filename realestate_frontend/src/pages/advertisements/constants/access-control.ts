/**
 * Access control scaffold for advertisements module.
 * Replace these placeholders with real RBAC/permission logic later.
 */
export function canViewAdvertisements(): boolean {
  return true;
}

export function canManageAdvertisements(): boolean {
  return true;
}
